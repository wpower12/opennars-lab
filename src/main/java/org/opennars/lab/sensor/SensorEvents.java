package org.opennars.lab.sensor;

public class SensorEvents {
    // A frame has been collected by the sensor.
    public static class FrameArrived{}

    // Emitted when a sensor is added to the system.
    public static class SensorStreamAdded{}
}
